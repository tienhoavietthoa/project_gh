{{!< home}}

<section class="p-8 bg-gray-50 min-h-screen flex justify-center items-center">
    <div class="container mx-auto max-w-5xl">
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <div class="bg-red-500 px-6 py-4 text-white text-center">
                <h3 class="text-3xl font-bold">{{product.name_product}}</h3>
            </div>
            <div class="p-6 flex flex-col md:flex-row">
                <div class="w-full md:w-1/2">
                    <img src="{{product.image_product}}" alt="{{product.name_product}}" class="w-full h-auto rounded-md shadow-md">
                </div>
                <div class="w-full md:w-1/2 md:pl-8 flex flex-col justify-between">
                    <div>
                        <p class="text-gray-700 mb-3">
                            <strong>Giá:</strong> <span class="text-red-600 font-bold text-lg">{{product.price}} VND</span>
                        </p>
                        <p class="text-gray-700 mb-3"><strong>Số lượng:</strong> {{product.quantity}}</p>
                        <p class="text-gray-700 mb-3"><strong>Kích thước:</strong> {{product.dimension}}</p>
                        <p class="text-gray-700 mb-3"><strong>Nhà sản xuất:</strong> {{product.manufacturer}}</p>
                        <p class="text-gray-700 mb-3"><strong>Số trang:</strong> {{product.page}}</p>
                        <p class="text-gray-700 mb-3"><strong>Tác giả:</strong> {{product.author}}</p>
                        <p class="text-gray-700 mb-3"><strong>Nhà xuất bản:</strong> {{product.publisher}}</p>
                        <p class="text-gray-700 mb-3"><strong>Năm xuất bản:</strong> {{product.publisher_year}}</p>
                        <p class="text-gray-700 mb-3"><strong>Mô tả sản phẩm:</strong> {{product.text_product}}</p>
                    </div>

                    <div class="mt-6 flex gap-4">
                        <button onclick="goBack()" class="bg-gray-500 text-white px-5 py-2 rounded-md shadow-md hover:bg-gray-600 transition">
                            ← Quay lại
                        </button>
                        <form action="/cart/add/{{product.id_product}}" method="POST">
                            <button type="submit" class="bg-red-600 text-white px-5 py-2 rounded-md shadow-md hover:bg-red-700 transition">
                                🛒 Thêm vào giỏ hàng
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    function goBack() {
        window.history.back();
    }
</script>
