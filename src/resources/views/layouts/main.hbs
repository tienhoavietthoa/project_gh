<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="flex h-screen gap-0">
        <!-- Sidebar -->
        {{> admin_sidebar}}
        
        <!-- Main Content -->
        <main class="flex-1">
            <!-- Top Navbar -->
            <header class="bg-white p-4 flex justify-between items-center">
                <div class="flex flex-col w-full text-center">
                    <!-- Lời chào mừng -->
                    <div class="text-xl font-semibold text-gray-700">
                        Xin chào, <span class="text-blue-600">{{adminName}}</span>! 👋 Chào mừng bạn đến với trang quản trị 🎉
                    </div>
                    <!-- Ngày giờ -->
                    <div class="text-lg text-gray-500 mt-1" id="current-time"></div>
                </div>
                <div class="flex items-center gap-6">
                    <button class="text-gray-600 text-xl">🔔</button>
                    <button class="text-gray-600 text-xl">
                        <i class="fas fa-envelope"></i>
                    </button>
                </div>
            </header>
            
            <!-- Dynamic Content -->
            {{{body}}}
        </main>
    </div>

    <!-- Script cập nhật ngày giờ -->
    <script>
        function updateTime() {
            const now = new Date();
            const options = { 
                weekday: 'long', 
                day: '2-digit', 
                month: '2-digit', 
                year: 'numeric', 
                hour: '2-digit', 
                minute: '2-digit', 
                second: '2-digit' 
            };
            document.getElementById('current-time').textContent = now.toLocaleString('vi-VN', options);
        }
        updateTime();
        setInterval(updateTime, 1000); // Cập nhật mỗi giây
    </script>
</body>
</html>
